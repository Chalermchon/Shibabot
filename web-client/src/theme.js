const { red } = require("chalk");

const theme = {
    colors: {
        primary: '#003DAF',
        success: 'green',
        warning: 'darkorange',
        error: 'red',
    }
}

export default theme